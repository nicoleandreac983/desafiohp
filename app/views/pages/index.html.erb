<h1 class="text-center">Personajes de Harry Potter</h1>

<table class="table table-dark table-striped ">
    <thead>
        <tr>
        <th class="bg-success">#</th>
        <th class="bg-success">Nombre</th>
        <th class="bg-success">Locación</th>
        <th class="bg-success">Casa</th>
        </tr>
    </thead>
    <tbody>
        <% @personajes.each_with_index do |character, index| %>
        <tr>
            <td><%= index + 1 %></td>
            <td><%= character.name %></td>
            <td><%= character.location %></td>
            <td><%= character.house %></td>
        </tr>
        <% end %>
    </tbody>
</table>